[pytest]
testpaths = tests
markers =
    slow: 원본 CSV(683MB)를 읽는 테스트. 빼려면 -m "not slow"
filterwarnings =
    # 394열짜리 원본에 컬럼을 붙이면 pandas가 조각난 프레임을 만든다.
    # 경고로 두면 넘어가게 되므로 실패로 올린다. 필요한 열만 새 표로 만들면 안 난다.
    error::pandas.errors.PerformanceWarning
